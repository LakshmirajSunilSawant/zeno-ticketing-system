# WHY committed: `pytest` with no arguments must do the right thing for anyone cloning the repo.
[pytest]
testpaths = tests
addopts = -q --strict-markers
filterwarnings =
    # python-jose calls the deprecated datetime.utcnow() internally; not our code, not actionable.
    ignore::DeprecationWarning:jose.*
